-  Конфигурация сетевого устройства
def timeout := 0x3E8;      -  Таймаут в мс
def retries := 0x03;       -  Количество попыток
def server_ip := "192.168.1.1";

list(
    $[timeout],
    $[retries],
    $[server_ip],
    list(0x01, 0x02, 0x03)  -  Дополнительные параметры
)